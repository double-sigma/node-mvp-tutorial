extends ../layout

block content
  form(action="/lead/" + lead.id + "/edit", method="POST")
    input(name="lead_email", type="email", placeholder="Enter email", value=lead.email)
    button(type="submit") Save